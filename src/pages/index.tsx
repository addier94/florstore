import { Chartjs2 } from "../components/home/Chartjs2";

const Home = () => {
  // if(homeBlogs.length === 0) return <Loading />;
  return (
    <>
      <div>
        <Chartjs2 />
        <Chartjs2 />
      </div>
    </>
  );
};

export default Home;
